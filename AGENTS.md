# リポジトリの目的

このリポジトリでは、TypeScript、およびその関連技術・エコシステムを学習するための資料を作成・管理する。

# 資料の配置

- すべての資料を`contents`ディレクトリ直下にフラットな構成で保存する。
- 資料がさらに増えた段階で、`contents`配下のディレクトリ構成を含めた整理・構造化を検討する。

# Frontmatter

各資料には YAML frontmatter を付与し、少なくとも次の項目を管理する。

```yaml
---
category: カテゴリ名
tags:
  - タグ名
date: YYYY-MM-DD
---
```

- `category`: 資料の主な分類を記載する。
- `tags`: 資料に関連するキーワードをリスト形式で記載する。
- `date`: 資料の作成日を `YYYY-MM-DD` 形式で記載する。

# カテゴリの運用

- 初期段階では、厳密なカテゴリ体系をあらかじめ固定しない。
- 記事が増えるにつれて、実際の内容や分類の傾向をもとにカテゴリを順次整理する。

# ライブラリ解説シリーズ

ライブラリの役割・特性を理解するための資料は、`contents/library-document-template.md`を下地として作成する。

## 読者と到達目標

- 対象読者は、TypeScriptの基本文法を理解し、対象ライブラリを初めて詳しく学ぶ開発者とする。
- APIの網羅ではなく、「なぜ存在するか」「何と比べて選ぶか」「設計が利用体験にどう表れるか」を理解できることを目標とする。
- 読者が上から順に読むだけで、背景から実装へ段階的に進める構成にする。
- 各セクションは独立したメモの寄せ集めにせず、前のセクションで提示した問いやトレードオフを後続のセクションで回収する。

## ファイル名

- ライブラリ名を小文字のkebab-caseにしたMarkdownファイルを`contents`ディレクトリ直下へ作成する。例: `contents/react-hook-form.md`。
- 同名の資料がある場合は新規作成せず、既存資料を更新する。

## 必須構成

ライブラリ解説は、原則として次の順序で構成する。

1. 概要
2. 歴史
3. 比較
4. 特徴
5. 仕組み
6. リポジトリ構成とソースコードの読み方
7. 公開されている実務事例
8. まとめ（番号は付けない）

対象ライブラリが非公開、source availableではないなど、セクション6を作成できない明確な理由がある場合は、その理由を短く記載する。情報が見つからないセクションを推測で埋めない。

## 1. 概要の観点

- 冒頭で、対象が何をするライブラリかを1〜2段落で説明する。
- 解決する問題、主な利用場所、入力と出力を明確にする。
- 最小限だが実務を想像できるサンプルコードを示す。
- 型アサーション、標準API、手書き実装など、初心者が混同しやすいものとの違いを説明する。
- 最後に、そのライブラリを一言で捉えるための中心的な見方を提示する。

## 2. 歴史の観点

- 年表だけでなく、ライブラリ登場前に存在した具体的なペインから始める。
- 「ペイン → 従来の解決策 → 従来策の限界 → 対象ライブラリの設計」という因果で説明する。
- 初期リリース以降も、重要な更新ごとに「当時起きていた問題・事象 → 既存設計で対応できなかった理由 → 導入された変更 → その変更で解決したことと残った課題」をつなげる。
- major versionや新機能を独立した箇条書きで並べず、前の設計判断が次の制約や要求へどうつながったかを、連続したストーリーとして説明する。
- 更新の背景には、性能上の制約、利用規模の拡大、開発者体験、他環境への展開、ecosystemからのfeedbackなど、確認できた具体的な要因を示す。
- 「機能が追加された」だけで終わらせず、その更新が利用者の設計、実装、migrationへどのような変化をもたらしたかを書く。
- 後の更新を先取りして正当化せず、各時点で公開されていた資料から当時の問題意識と判断を確認する。
- 必要に応じて、JavaScript・TypeScriptの標準機能、先行ライブラリ、他言語の類似概念を取り上げる。
- 初回リリース、major version、重要な設計変更など、現在の理解に必要な節目だけを選ぶ。
- 日付、version、歴史的経緯はrelease notes、changelog、作者の発言などの一次資料で確認する。
- 章末では、選んだ節目を貫く設計思想を再提示し、個別機能の年表ではなく設計の積み重ねとしてまとめる。

## 3. 比較の観点

- 比較対象は原則2〜4個とし、設計上の重心や実務上の選択理由が異なるものだけを選ぶ。
- 類似ライブラリを大量に列挙しない。
- 最初に比較表を置き、少なくとも次の列を設ける。
  - ライブラリ名と公式GitHubリポジトリへのリンク
  - GitHub Stars
  - 設計の中心
  - 強い場面
  - 主なトレードオフ
- GitHub Starsは公式リポジトリで確認し、`約43.5k`のような概数と調査日を記載する。
- Starsは人気や情報量の参考であり、品質や適合性を直接示さないことを明記する。
- 表の後では比較対象ごとに、API例、選ぶ理由、選ばない理由を文章で掘り下げる。
- 「高速」「軽量」「型安全」など各ライブラリが共通して主張する性質を、根拠なく固有の優位性として扱わない。
- performanceを比較する場合は、公式または再現可能なbenchmarkの条件を示し、異なる条件の数値を直接比較しない。

## 4. 特徴の観点

- 機能一覧ではなく、比較で明らかになったpros/consと結びつく特徴を3〜6個選ぶ。
- 各特徴を「仕組みまたはAPI例 → 利点 → 代償・注意点」の順で説明する。
- 他ライブラリにも一般的な性質と、対象ライブラリに特徴的な設計を区別する。
- 宣伝文句をそのまま結論にせず、どの条件で価値が出るかを書く。

## 5. 仕組みの観点

- 詳細な逐行解説より、主要コンポーネントとデータ・制御の流れを優先する。
- 最初に、主要コンポーネントについて「責務」「主な入力」「主な出力または保持する状態」「他コンポーネントとの関係」を表に整理する。
- 利用者が扱う値、内部表現、実環境のobjectなど、似ているが役割の異なる概念を区別する。
- 入力から出力またはエラーまでの全体像をMermaidで示す。単純な補足にはテキスト図を使ってもよい。
- 一つの図にすべての要素を詰め込まない。package・module間の依存、通常処理のsequence、state遷移、error経路など、理解したい関係ごとに図を分割する。
- 図は本文の代替にせず、各nodeや矢印が表す責務と相互作用を図の前後で説明する。
- 構造や依存関係には`flowchart`、呼び出し順には`sequenceDiagram`、処理状態の変化には`stateDiagram-v2`など、関係に適したMermaid diagramを選ぶ。
- packageや主要moduleごとに、どの層が公開API、core logic、platform固有処理、scheduling・I/Oなどを担当するかを示す。
- 公開APIのサンプルから内部の主要概念へ対応付ける。
- 短いAPI呼び出しを一つ選び、「受付・登録 → core処理 → stateまたは中間表現の更新 → 出力への反映」という段階に分けて追う。
- 各段階で、呼び出す側、呼び出される側、受け渡すdata、保持または変更されるstateを明記する。
- 同期処理と非同期処理、準備段階と反映段階、中断・再試行の可否など、実行上重要なphaseがあれば表または図で区別する。
- 成功経路だけでなく、validation error、例外、fallback、retry、cleanupなど、利用者の設計へ影響する失敗・回復経路も示す。
- 重要なclass、function、data structureについて、公式GitHub上の実装ファイルへリンクする。
- GitHubリンクがdefault branchを指す場合は変更され得ることを明記し、特定versionの調査ではtagまたはcommit SHAを使うよう案内する。
- 内部APIは公開APIではなく、version間で変更され得ることを明記する。

## 6. リポジトリ構成とソースコードの読み方の観点

- 公式リポジトリがmonorepoなら、最初に対象packageの位置を示す。
- 主要ディレクトリだけの小さなtreeを載せ、各ディレクトリの責務を1行で説明する。
- 読解順は、entry pointから公開API、core、error、testへ進むように段階化する。
- 一つの短いAPI呼び出しを題材に、どのファイルをどの順に追うか示す。
- 全ファイルを順番に読むことは勧めず、最初は対象外にしてよい領域も示す。
- 最後に「理解したいこと → 読むファイル」の対応表を置く。
- tests、examples、benchmarkが仕様理解に使える場合は、その場所と検索するAPI名を案内する。

## 7. 公開されている実務事例の観点

- 原則2〜4件に絞る。十分な事例がなければ件数を満たすために質の低い記事を加えない。
- 企業テックブログ、当事者が書いたZenn・Qiita記事、conference資料など、実際の導入経験が確認できるものを優先する。
- 基本APIを並べただけの記事、出典が不明な記事、具体的経験のないSEO目的の記事は採用しない。
- 用途、導入前の問題、設計判断、導入方法、結果、残った課題のうち、複数が具体的に書かれた記事を選ぶ。
- 同じ用途の記事だけで揃えず、導入、運用、migration、障害対応など異なる観点を組み合わせる。
- 各事例は、記事へのリンク、背景、採用した設計、結果、読者が持ち帰る教訓を要約する。
- 数値的な成果は記事内に明記されたものだけを使い、「記事によれば」など出典主体が分かる表現にする。
- 古いmajor versionの記事は、現在も有効な設計知識と古くなったAPIを区別する。

## まとめの観点

- 各章の要約を繰り返すのではなく、対象ライブラリを捉える中心的な考え方を再提示する。
- 比較対象との選択基準を短く整理する。
- 実務で導入するときに最初に決めるべき境界や運用方針を示す。

# 調査と出典

- 技術仕様、version、互換性、内部構造は、公式ドキュメント、公式GitHub、release notes、標準仕様などの一次資料を優先する。
- 比較対象についても、それぞれの公式資料を確認する。一方のライブラリが書いた比較表だけを根拠にしない。
- 実務事例では当事者の記事を一次資料として扱う。
- 時点により変わる情報には調査日または対象versionを添える。
- 本文の主張を確認できるページへ直接リンクし、検索結果ページにはリンクしない。
- 出典の文章を長く転載せず、自分の言葉で要約する。
- 推測と資料で確認できた事実を分ける。推測が不要なら記載しない。

# コード例

- 特に断りがなければ、執筆時点のstable major versionを使う。
- importを含め、コード片単体で文脈が分かるようにする。
- サンプル用の未定義変数を除き、可能な範囲で型検査できるコードにする。
- 初出のAPIを一度に詰め込みすぎず、その段落で説明する概念へ絞る。
- 非推奨API、過去versionのAPI、実験的APIには明示的な注記を付ける。
- エラー処理を省略したコードを実務例として提示しない。省略する場合は学習用の簡略化だと明記する。

# 文体と表記

- 読み物として自然な「である」調を基本とする。
- ライブラリ名、API名、class名、file名は公式表記を使う。
- 英語の技術語は必要に応じて日本語の意味を添え、同じ概念の表記を記事内で統一する。
- 過度な強調、煽り、根拠のない最上級表現を避ける。
- 「便利」「簡単」で終わらせず、何と比べて、どの作業やリスクがどう変わるかを書く。

# 完成時の確認

資料を完成とする前に、次を確認する。

- frontmatterに`category`、`tags`、`date`がある。
- 必須の7セクションとまとめが順番どおりにある。
- 冒頭のコードから対象ライブラリの役割を理解できる。
- 歴史が登場前のペインから始まり、各更新の「問題・限界・変更・結果」が次の節目へつながるストーリーになっている。
- 歴史上の節目が一次資料で確認され、後知恵による推測ではなく当時の問題意識として説明されている。
- 比較対象が少数に絞られ、選択理由とトレードオフがある。
- GitHub Starsに調査日と注意書きがある。
- 特徴が比較のpros/consと対応している。
- 仕組みに主要コンポーネントの責務・入出力・相互関係を示す表がある。
- 仕組みの全体像と主要な制御・データフローが、目的別に分割したMermaid図で示されている。
- 仕組みで公開APIから内部処理、出力への反映、エラーまたは回復経路までを追え、公式source fileへのリンクがある。
- 実務事例が当事者による具体的な記事で、要約に出典以上の主張を加えていない。
- source codeの読解順がentry pointから具体的に示されている。
- 時点依存の情報に日付またはversionがある。
- code fence、table、linkなどのMarkdown構文が壊れていない。
